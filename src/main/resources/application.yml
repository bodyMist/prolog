logging:
  level:
    root: info
spring:
  messages:
    basename: errors
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:mysql://localhost:3306/prolog-dev?serverTimezone=Asia/Seoul
    username: root
    password: 8344
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true